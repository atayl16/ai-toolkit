{
  "name": "Rails + Postgres",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "features": {},
  "forwardPorts": [3000, 5432],
  "mounts": [
    "source=${env:HOME}/.ai-toolkit,target=/ai,type=bind,consistency=cached,readonly"
  ],
  "postCreateCommand": "bin/setup || true"
}
